[package]
name = "vol1-cap-19-dp"
version = "0.1.0"
edition = "2024"
description = "Vol.I Cap.19 — DP en grafos: longest path DAG + tree DP (subtree sizes) + Held-Karp TSP"
license = "CC-BY-NC-SA-4.0"
authors = ["Rubentxu"]
publish = false

[lib]
path = "src/lib.rs"

[lints.clippy]
all = { level = "warn", priority = -1 }

[dependencies]
petgraph = "0.6"