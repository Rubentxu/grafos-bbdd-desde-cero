[package]
name = "vol2-liradb"
version = "0.1.0"
edition = "2024"
description = "Vol.II Caps 7-20 — LiraDB: Property Graph + GraphStore + encoding + append-only + páginas + Pager/FilePager + BufferPool (Clock/LRU) + CSR persistente + HashIndex + B+ tree + mantenimiento (inspect|check|compact) + lenguaje LiraQL (AST cap 17 + lexer y parser cap 18 + plan lógico cap 19 + motor de ejecución Volcano con operadores iteradores, métricas y run() parse→lower→execute cap 20)"
license = "CC-BY-NC-SA-4.0"
authors = ["Rubentxu"]
publish = false

[lib]
path = "src/lib.rs"

[lints.clippy]
all = { level = "warn", priority = -1 }

[dependencies]

[dev-dependencies]
# Para tests de FilePager (cap 12): directorios temporales.
# Pinned sin ^ ni ~ por la política de reproducibilidad del workspace.
tempfile = "3.10"