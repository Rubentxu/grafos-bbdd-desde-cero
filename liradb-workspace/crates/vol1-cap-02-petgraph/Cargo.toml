[package]
name = "vol1-cap-02-petgraph"
version = "0.1.0"
edition = "2024"
description = "Vol.I Cap.2 — Mismo grafo de §2.5 pero con petgraph"
license = "CC-BY-NC-SA-4.0"
authors = ["Rubentxu"]
publish = false

[lib]
path = "src/lib.rs"

[lints.clippy]
all = { level = "warn", priority = -1 }

[dependencies]
# petgraph 0.6 — versión histórica del Vol.I; mantenida para reproducibilidad.
# Si fuera a actualizar: revisar CHANGELOG.md de petgraph; hay breaking changes
# entre 0.6 y 0.7 (módulos reorganizados, NodeIndex::new).
petgraph = "0.6"